typealias если название слишком длинное, можно его заменить на alias
ArrayList<T>- псевдоним ArrayList для  Java java.util.ArrayList<E>
Используется из Java java.util.ArrayList<E>

ИСПОЛЬЗУЕТ СТАТИЧЕСКИЙ МАССИВ. И мы уже его динамически расширяем

Для выделения памяти нужно 2 Обязательных параметра
тип данных - определеяет какое число байт нужно на один элемент
количество элементов

Статический массив - это элементарная операция выделения памяти под набор элементов с заданным размером.
Array в kotlin = Integer[size] в java

IntArray - заполнит значениями по умолчанию, те 0

Проблема Array
 - нет метода удаления и добавления элементов
 - размер статичен и его надо знать заранее и нельзя менять


ArrayList - обертка для Array из Java. У Kotlin нет своих коллекций, используют Java-коллекции
Изначально 10 размер. Но size=0
Если полностью заполнен, то увеличивается в 2 раза

Под капотом использует Array, поэтому ArrayList
Есть плюсы в виде операций O(1)
Есть минусы O(n)
Если удалим много элементов, то его capacity не будет уменьшаться и также будет резервировать больщой объем памяти

Какую колекцию будете использовать, если нужно хранить большое количество данных
Зависит от того, что чаще с ней будем делать. 

Каким образом метод перестановки элементов был оптимизирован?
Но алгоритмическая сложность не изменяется.
Для сдвига или копирования элементов используется системный метод arraycopy, написанный на языке C или C++(помечен как native), низкоуровневая реализация, которая быстрее чем копирование в обычном цикле. Более оптимизирована. 