https://www.mockaroo.com/ - сайт для mock-данных

Обратная связь от пользователей

Unit-тесты

Тестирование с помощью тестировщиков

Код-ревью - это процесс проверки кода

на наличие ошибок (пропущенное требование, неверно  
обработанный краевой случай)  

наличие неэффективных решений - качество кода. хороший код — масштабируемый, переиспользуемый, легко  
поддерживаемый и читаемый  

Поддерживать принятые в команде правила (архитектуру, договорённости по  
использованию фреймворков и прочее).  

Быстрее адаптировать новых разработчиков, чтобы они вместе с командой  
проходили период «притирки», понимали, как уже принято писать код и так далее.  

способ посмотреть на проект глубже

посмотреть на то как другие пишут код.Развивать команду: учиться новым фишкам, просто читая код коллеги или  
получая ценную рекомендацию.  

Контролировать bus factor (термин "bus factor" используют, чтобы описать  
вероятность того, что проект продолжит развиваться даже при отсутствии одного  
или нескольких разработчиков): увеличивать число человек, знающих каждый  
элемент системы

### JUnit
библиотека для создания тестов

Каждый тестовый сценарий выполняется в отдельном методе
 - Имя класса теста соответствуеют тестируемому классу
 - Имя метода заключается в одинарные кавычки и пишет, что именно тестирует прям в предложении
 - Каждый тестовый метод помечается аннотацией @Test
 - Для проверки дробных чисел ОБЯЗАТЕЛЬНО нужно использовать погрешность, которая является удовлетворительной, потому что если умножить 0.01\*100=!1 всегда
```kotlin
 @Test  
fun `When 0,01 multiply 100 Then result equals 1(double) with delta 0,001`() {  
    val actual = Calculator().multiply(0.01, 100.0)  
  
    //должно быть 1, но не 1 на самом деле, а 1.0000000000000007  
    // Нужно погрешность указывать    var aDouble = 0.0  
    repeat(100) {  
        aDouble += 0.01  
    }  
  
    val expected = aDouble  
    val delta = 0.001  
  
    assertEquals(expected, actual, delta)  
}
```
 - Для проверки полученного и ожидаемого значения используется метод assertEquals()

 