В JetpackNavigation навигация является частью UI-слоя и проприсывается прямо в compose-функциях
В Decompose UI ничего не знает  о навигации
2. viewModel создаем снаружи и передаем ее в compose-функции
3. Decompose для kotlin мультиплатформ

В decompose есть понятие компонента - элемента, который описывает логику экрана без UI - содержит в себе поля(данные) и методы(кнопки). Описывается в виде интерфейса, а затем реализуется в классе Default..Component

scope в component нужно удалять, во viewModel удалялась автоматические
viewModel переживала смену конфигурации
Решение componentContext