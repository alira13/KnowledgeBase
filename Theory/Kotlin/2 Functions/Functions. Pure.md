Чистые функции - 
 - при одних и тех же входных параметрах всегда один и тот же выходной результат
 - не содержат side-эффектов
 - не используют мутабельные объекты(mutable и var)

Контекст функции - ее тело
Side-эффект - когда функция меняет что-то за пределами своего контекста(тела)
