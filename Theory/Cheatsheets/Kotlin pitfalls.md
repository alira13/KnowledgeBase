1. Для списков используй backingField. Делай публичные списки и множества val и неизменяемыми. Иначе кто угодно может извне изменить твою коллекцию.

2. Используй get, так как он позволяет каждый раз получать актуальный список, а не тот, который был получен при инициализации.
```kotlin
class Employee(val id: Int, val name: String) {  
    private val _tasks = mutableListOf<Task>()  
    val tasks  
        get() = _tasks.toList()
}
```

1. Думай о том, как работает contains. Сравнивает по ссылке или по значениям полей. Переопределяй hashCode и Equals для корректного сравнения. Иначе сравнение будет работать не так, как ты задумал. По возможности используй data-классы, тогда сравнения будут корректно работать.

2. Не используй var свойства для классов. Когда ты создашь коллекцию из объектов этого класса, ЛЮБОЙ сможет в ЛЮБОМ месте поменять поле элемента списка и оно также поменяется в твоем списке.

3. Не изменяй поля элементов списка. Используй метод copy. Скопируй-замени поле-удали из списка старый элемент-добавь новый. Этот вывод следует из предыдущего пункта.
```kotlin
fun modifyTaskDetails(taskId: Int, newTitle: String, newDescription: String) {  
    val task = _tasks.find { it.id == taskId }  
    if (task != null) {  
        _tasks.remove(task)  
        _tasks.add(task.copy(title = newTitle, description = newDescription))  
        println("Детали задачи ${task.id} обновлены.")  
    } else {  
        println("Задача с ID $taskId не найдена.")  
    }  
}

```

1. Вегда помни о циклическом переполнении у типов. У short после 127 идет -128. Все население Земли не влезет в Int

2. Переопределяй toString(). Это удобно для логирования

3. Старайся по максимуму делать все переменные приватными. Создавай методы для корректной работы с приватными полями, это более контролируемо.

4. Старайся по максимуму все публичные переменные делать val и unmutable. 

5. Используй Set где это допустимо вместо List, это быстрее.
6. Equals при null может выбросить исключение, а == нет.
7. lateinit или nullable? Исключение в runtime или постоянная обработка nullable? Лучше воздержаться от lateinit по возможности. Может стоит использовать by lazy?
8. Не забывай удалять binding в onDestroy()
9. В СУПЕР крайних случаях используй !! для работы с nullable-типами. Это не безопасный вызов. Когда мы сами ручаемся за то, что там не будет null. Но мы не можем этого знать наверняка.
10. Не используй часто try-catch. Старайся просто через if проверить все варианты. Сообщение "Что-то пошло не так" - это крайний случай
11. Всегда добавлять@SerialName при сериализации в поля классов. Для ясности и читаемости+после обфускации(кодирования имен кода) имена ключей и переменных будут отличаться, и код перестанет работать.
12. Используй потокобезопасные Singleton